name: Auto Release PR

on:
  push:
    branches:
      - develop

permissions:
  contents: write
  pull-requests: write

jobs:
  auto-release:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Fetch all
        run: git fetch --all

      - name: Create clean release branch from develop
        run: |
          git checkout develop
          git pull origin develop
          
          # 💥 强制创建干净分支（此分支永远从 develop 来）
          git branch -f release/auto-release develop
          git push -f origin release/auto-release

      # 🚨 核心修复：create-pull-request 必须使用自己的临时分支，不再用 release/auto-release
      - name: Create PR from a TEMP branch
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          base: master
          branch: auto-release-pr-temp          # ← 临时分支，避免 rebase 污染
          delete-branch: true                   # 创建后自动删除临时分支
          title: "Release: Next Version"
          body: |
            ## 🚀 Auto Release PR (develop → master)

            Release branch:
            **release/auto-release**

            ✔ After merging this PR, semantic-release will run automatically.
          labels: |
            release
